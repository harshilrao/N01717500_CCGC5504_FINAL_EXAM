spec:
  template:
    spec:
      containers:
      - name: nginx-exporter
        image: nginx/nginx-prometheus-exporter:0.10.0
        args:
          - "-nginx.scrape-uri=http://localhost/status"
        ports:
        - containerPort: 9113
          name: metrics
